import turtle

t = turtle.Turtle()


class TurtleDrawing:

    def graph(t):
        # draws the graph
        t.right(135)
        t.up()
        t.forward(200)
        t.left(135)
        t.down()
        t.forward(500)
        t.backward(500)
        t.left(90)
        t.forward(500)
        t.down(500)
        t.right(90)

    # def hairColour_Graph(t):
